--  Create Table(s) --
CREATE TABLE Files (
    [id] INT NOT NULL PRIMARY KEY,
    [originalName] VARCHAR(120) NOT NULL,
    [pathName] VARCHAR(120) NOT NULL, 
);

-- Test --
INSERT INTO Files (id, originalName, pathName) VALUES
('1', 'testFile.txt', '/files/testFile.txt');

DROP TABLE Messages;